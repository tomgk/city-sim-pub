/**
 * Vacant buildings.
 * Represented as different classes because the same vacant buildings can be
 * used for multiple zones and just creating a building type instance for each
 * possible variant seems bad.
 */
package org.exolin.citysim.model.building.vacant;
